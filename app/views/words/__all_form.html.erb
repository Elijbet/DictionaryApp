

<!-- Nav tabs -->
  <ul class="nav nav-tabs" role="tablist">
    <li class="nav-item">
      <a class="nav-link active" data-toggle="tab" href="#word" role="tab">
      <h6>contribute new</h6><h5>WORD</h5><h6>with or w/o</h6><h5>DEFINITION</h5>
      </a>
    </li>
    <li class="nav-item">
      <a class="nav-link" data-toggle="tab" href="#definition" role="tab">
        <h6>select known word, to add</h6><h5>DEFINITION</h5>
      </a>
    </li>
  </ul>

  <!-- Tab panes -->
  <div class="tab-content">
    <div class="tab-pane active" id="word" role="tabpanel">

      <%= render 'form', word: @word %>

    </div>
    <div class="tab-pane" id="definition" role="tabpanel">

      <%#= render 'definition_form', word: @word %>

      <%= form_for @word do |form| %>

  <% if word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(word.errors.count, "error") %> prohibited this word from being saved:</h2>

      <ul>
      <% word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group upper-margin">
    <%= select_tag :word, options_for_select(Word.all.map { |word| [word.word, word.id] }), include_blank: "Word", class: "form-control"%>
  </div>

  <%= form.fields_for :definitions do |definition| %>

    <div class="form-group">
      <%= definition.text_field :definition, class: "form-control definition", placeholder: "Definition", id: "chartdiv"%>
    </div>

  <% end %>

  <div class="form-group">
    <%= form.submit class: "btn btn-secondary form-control", value: "Create Definition" %>
  </div>

<% end %>

    </div>
  </div>


